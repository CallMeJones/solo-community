# Solo daemon HTTP API base URL. Defaults to http://127.0.0.1:17821 when unset.
VITE_SOLO_API_URL=http://127.0.0.1:17821

# Set to 1 to use deterministic mock graph data instead of live Solo fetches.
# VITE_SOLO_USE_MOCKS=1
